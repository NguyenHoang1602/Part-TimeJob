const config = {
    screens: {
        WelcomeScreen: {
            path : 'welcome',
        },
    },
};

const linking = {
    prefixes: ['ParttimeJob://app'],
    config,
};

export default linking;